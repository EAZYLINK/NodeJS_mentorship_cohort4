function mul(a, b) {
    return a * b;
}

function add(a, b) {
    return a + b;
}

module.exports = {
    mul,
    add
};

// exports.mul = (a, b) => a * b;

// export const mul = (a, b) => a * b; // ES6